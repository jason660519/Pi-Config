#!/usr/bin/env python3
"""Thin entrypoint — loads ../src/guard.py and dispatches."""
import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parent.parent / "src"))
from guard import main  # noqa: E402

sys.exit(main())
